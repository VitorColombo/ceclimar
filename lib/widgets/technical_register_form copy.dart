import 'package:flutter/material.dart';

class TechnicalRegisterForm extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Technical Register Form'),
    );
  }
}